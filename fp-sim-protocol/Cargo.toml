[package]
name = "fp-sim-protocol"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0"

[dependencies]
postcard = { version = "1.1.3", features = ["use-std"] }
serde = { version = "1.0.219", features = ["derive"] }

[lints.rust]
unused_must_use = "deny"
