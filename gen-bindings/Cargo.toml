[package]
name = "gen-bindings"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0"

[dependencies]
libfp = { path = "../libfp" }
postcard-bindgen = { version = "0.7.1", features = ["generating"] }
syn = { version = "2", features = ["full", "extra-traits"] }

# Keep this generator standalone even when the checkout itself is nested in a
# Git worktree directory inside another Faderpunk workspace.
[workspace]
